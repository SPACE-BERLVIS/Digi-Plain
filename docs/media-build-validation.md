# Media publisher build validation

This temporary smoke-test note verifies the latest `main` merge ref after adding Telegram screenshot uploads and the pinned Wrangler toolchain.

CI must pass Astro strict checks, the production static build, and the Cloudflare Worker dry-run before this validation is merged.
