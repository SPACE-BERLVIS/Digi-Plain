{
  "name": "digiplain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "deploy": "wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run",
    "publisher:dev": "npm run build && wrangler dev",
    "telegram:inspect": "node scripts/telegram-inspect-updates.mjs",
    "telegram:set-webhook": "node scripts/telegram-set-webhook.mjs"
  },
  "dependencies": {
    "astro": "^6.4.8"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.8.3",
    "wrangler": "4.114.0"
  }
}
