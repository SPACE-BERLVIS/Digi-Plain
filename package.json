{
  "name": "digiplain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "deploy": "npx wrangler@latest deploy",
    "publisher:dev": "npm run build && npx wrangler@latest dev",
    "telegram:set-webhook": "node scripts/telegram-set-webhook.mjs"
  },
  "dependencies": {
    "astro": "^6.4.8"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.8.3"
  }
}
